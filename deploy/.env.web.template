# PhysioLens Web — Production Environment Variables
# Copy to /opt/physiolens/.env.web on the server and fill in values.
# Note: NEXT_PUBLIC_* vars are baked in at Docker build time via build args
# in docker-compose.prod.yml. This file provides server-side runtime vars.

# AI Interpretation (provide at least one)
OPENAI_API_KEY=sk-proj-...
# OPENAI_MODEL=gpt-4o
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Stripe (publishable key is set as a build arg in docker-compose.prod.yml)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
